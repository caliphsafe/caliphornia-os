export default function FriendsBuilderPage() { return <main className="shell stack"><header className="topbar"><div><span className="eyebrow">Admin</span><h1 className="h1">fri.ends Builder</h1></div><a className="btn" href="/dashboard">Dashboard</a></header><section className="glass card"><p className="muted">Use `/api/dashboard/friends-builder` to safely upsert canonical `conversations` and `conversation_messages` records. The legacy `friends_conversations` table is not extended.</p></section></main>; }
