CALIPHORNIA OS — REQUIRED EXISTING-FILE EDITS

1. app/globals.css
Append the complete contents of APPEND-TO-app-globals.css to the END of the file.


2. components/StatsPageClient.tsx
Find the five bottom navigation buttons and place them in this exact order:

  Summary
  Apps
  Places
  Share
  Rankings

Add this class to the navigation wrapper:

  className={`${styles.bottomNav} stats-five-tab-nav`}

If its existing CSS module class has another name, preserve that class and add
stats-five-tab-nav beside it.

Rankings must remain the fifth and final button.


3. components/FriendsThreadClient.tsx

Add this import:

  import InlineSongShareLink from "@/components/music/InlineSongShareLink";

In renderAudio(), immediately AFTER the closing </button> for
friends-original-audio-card and still inside friends-original-message-group,
add:

  {clip.playlist_song_slug ? (
    <div className="friends-original-audio-actions">
      <InlineSongShareLink
        songId={clip.asset?.linked_song_id || null}
        songSlug={clip.playlist_song_slug}
        title={
          clip.playlist_song_title ||
          msg.audio_label ||
          clip.clip_title
        }
      />
    </div>
  ) : null}

Also change the bottom anchor from:

  <div ref={bottomAnchorRef} style={{ height: 120 }} />

to:

  <div ref={bottomAnchorRef} style={{ height: 24 }} />

The new sticky composer and normal-flow message area no longer need a 120px
artificial spacer.


4. components/FartherhoodClient.tsx

Add this import:

  import InlineSongShareLink from "@/components/music/InlineSongShareLink";

For EVERY rendered track/song row, place this beside the existing play/open
controls:

  <InlineSongShareLink
    songId={track.id || null}
    songSlug={track.slug}
    title={track.title}
  />

Do this in the map that renders tracks so every FarTHErHOOD song receives it,
not only the currently selected song or modal.


5. components/MiliaSongCard.tsx

This component already has a Share control. Confirm its handleShare navigates
to /apps/share with songId/songSlug and does not call /api/share/start directly.

The Share control must appear on every MiliaSongCard generated by the page.


6. components/FriendsInboxLoader.tsx or Friends inbox song cards

If the Friends inbox itself renders any final-song card outside the thread,
add the same InlineSongShareLink to each final song:

  <InlineSongShareLink
    songId={song.id || null}
    songSlug={song.slug}
    title={song.title}
  />

Do not add Share to plain conversations that do not represent a song.


UNIVERSAL NAVIGATION RULE

Do not create page-specific Share/Account pills, floating controls, or alternate
link styles. The reusable GlobalQuickActions footer is the single universal
Home / Music / Share / Account navigation pattern across apps.

Page-specific song Share actions use InlineSongShareLink.
